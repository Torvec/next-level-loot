meta {
  name: Get a list of visually similar games (available only for business and enterprise API users)
  type: http
  seq: 12
}

get {
  url: {{rawgURL}}/games/:id/suggested?key={{rawg-key}}
  body: none
  auth: none
}

params:query {
  key: {{rawg-key}}
}

params:path {
  id: 
}
